"use strict";
/////////////////////////
// Script
/////////////////////////
function	getTetrisStruct()
{
	const struct = {
		name: "TETRIS",
		canvas: undefined,
		ctx: undefined,
		run: startTetris,
		running: 0
	};
	return (struct);
}

async function	startTetris(struct)
{
	// Init game
	// Init EventListener
	// canvas.focus();
	// Loop game;

	// at the end of the game:
	//	game.running = 0;
	//	endGame(struct);
}
